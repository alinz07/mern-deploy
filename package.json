{
	"name": "mern-deploy",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"install-server": "npm ci && npm --prefix client ci && npm --prefix client run build && npm --prefix backend ci",
		"start-server": "NODE_ENV=production node backend/app.js",
		"install-client": "cd client && npm install",
		"build-client": "cd client && npm run build",
		"start-client": "cd client && npm run start"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"bcryptjs": "^3.0.2",
		"jsonwebtoken": "^9.0.2",
		"react-router-dom": "^7.6.0"
	}
}
