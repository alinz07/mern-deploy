{
	"name": "mern-deploy",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"install-server": "cd backend && npm install",
		"start-server": "cd backend && node app.js",
		"install-client": "cd client && npm install",
		"build-client": "cd client && npm run build",
		"start-client": "cd client && npm run start",
		"dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix client\"",
		"dev:install": "npm run install-server && npm run install-client"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"bcryptjs": "^3.0.2",
		"dotenv": "^17.2.3",
		"jsonwebtoken": "^9.0.2",
		"react-router-dom": "^7.6.0"
	},
	"devDependencies": {
		"concurrently": "^9.0.0",
		"nodemon": "^3.0.0"
	}
}
